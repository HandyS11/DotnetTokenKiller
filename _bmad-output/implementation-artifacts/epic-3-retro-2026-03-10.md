# Epic 3 Retrospective — Restore, Publish & Pack Filters

**Date:** 2026-03-10
**Facilitator:** Bob (Scrum Master)
**Participants:** Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), HandyS11 (Project Lead)
**Epic:** Epic 3 — Restore, Publish & Pack Filters
**Status:** Complete

---

## Epic Summary

| Metric | Value |
|---|---|
| Stories completed | 3/3 (100%) |
| New tests added | +28 (74 → 102 total) |
| Build | 0 errors, 0 warnings |
| Format | Clean |
| Debug incidents | 2 minor (3.1 + 3.2), 0 in 3.3 |
| Technical debt introduced | 0 |
| Token savings — restore | ≥90% ✅ |
| Token savings — publish | ~86% (≥80% threshold) ✅ |
| Token savings — pack | ~92% (≥85% threshold) ✅ |
| Production incidents | 0 |

### Stories Delivered

| Story | Description | Debug Incidents |
|---|---|---|
| 3.1 | Implement dotnet restore Filter with Tests | 1 minor (RCS1134, import order) |
| 3.2 | Implement dotnet publish Filter with Tests | 1 minor (S3358 nested ternary) |
| 3.3 | Implement dotnet pack Filter with Tests | 0 |

---

## What Went Well

- **Straightforward execution** — All three stories delivered without drama; the filter pattern was proven enough that no structural decisions were needed mid-epic
- **Debug incident arc: 1 → 1 → 0** — Continuous improvement across stories; S3358 nested ternary surprised in 3.2, was immediately documented as a pitfall, and 3.3 had zero incidents as a result
- **MEMORY.md flywheel** — Pre-loaded analyzer pitfalls from prior epics eliminated surprises; the investment compounds with each epic
- **Pattern reuse** — `DotnetPackFilter` was structurally identical to `DotnetPublishFilter`; "copy and adapt" strategy delivered with minimal friction
- **100% delivery** — 3 stories, 28 new tests, zero regressions, zero technical debt
- **All token savings targets met** — restore ≥90%, publish ~86%, pack ~92%

---

## Challenges

- **S3358 nested ternary** — Unexpected analyzer warning in `BuildContext` in Story 3.2; required extracting `projectSuffix` variable; not anticipated from prior stories
- **Empty-suite fixture carry-forward** — Action item from Epic 2 retro (add `dotnet_test_zero.txt` fixture to `DotnetTestFilterTests`) slipped a second time; addressed in action items below with a named sprint task

---

## Key Insights

1. **"Straightforward" is an earned outcome** — It signals the filter architecture has matured to a stable, repeatable pattern; Epic 4's larger story count should benefit from the same flywheel
2. **Immediate pitfall documentation pays off immediately** — S3358 fix documented after 3.2, absent from 3.3; the lag between learning and applying has shrunk to one story
3. **Zombie action items need a named task or a tombstone** — Carrying the empty-suite fixture forward twice without a specific sprint task is the failure mode; the fix is naming it explicitly

---

## Previous Retrospective Follow-Through (Epic 2)

| # | Action Item | Status | Evidence |
|---|---|---|---|
| 1 | Add `dotnet_test_zero.txt` fixture + zero-tests test to `DotnetTestFilterTests` | ❌ Not addressed | Not mentioned in any Epic 3 story record; carried forward to Epic 4 sprint task |
| 2 | Flip `epic-2` status → `done` in sprint-status.yaml | ✅ Done | sprint-status.yaml shows `epic-2: done` |
| Prep 1 | Research restore/publish/pack output formats; capture fixture files | ✅ Done | Fixture files created before each story dev started |
| Prep 2 | Clarify output path surfacing for publish/pack before stories 3.2/3.3 | ✅ Done | Requirements clear in story specs; no mid-story drift |

---

## Next Epic Preview: Epic 4 — More dotnet Filters + Passthrough

**Stories:** 6 (clean, run, ef, format, nuget, passthrough for unrecognized subcommands)

**Dependencies on Epic 3:**

- `IOutputFilter` contract ✅ stable
- `FilteredRunUseCase` orchestrator ✅ complete
- DI singleton registration pattern ✅ established
- Verify.Xunit snapshot workflow ✅ proven
- `DotnetPublishFilter` / `DotnetBuildFilter` diagnostic grouping pattern ✅ available for reuse

**Notable story considerations:**

- `dotnet run` filter is architecturally distinct — must strip build preamble but **preserve application output intact** (60–80% savings); SM must explicitly spec this behavior in ACs
- Passthrough command (Story 4.6) has no filter — pure routing for unrecognized subcommands; SM must flag clearly to avoid misapplied filter pattern
- 6 stories is double Epic 3's count; same story-by-story rhythm applies

**Preparation approach:** Same as Epic 3 — no dedicated prep sprint; capture fixture before each story dev starts.

---

## Action Items

| # | Action | Owner | Priority |
|---|---|---|---|
| 1 | Add `dotnet_test_zero.txt` fixture + fixture-driven zero-tests test to `DotnetTestFilterTests` | HandyS11 | Sprint 4 (before or alongside Story 4.1) |
| 2 | Flip `epic-3` status → `done` in sprint-status.yaml | HandyS11 | Immediate housekeeping |

## Epic 4 Preparation Tasks

| # | Task | Owner | Notes |
|---|---|---|---|
| 1 | Capture fixture files for each command before story dev starts | HandyS11 | Same discipline as Epic 3 |
| 2 | SM: explicitly spec "preserve app output" behavior in `dotnet run` story ACs | SM agent | Prevents mid-story requirement ambiguity |
| 3 | SM: flag passthrough story (4.6) as no-filter routing — not a standard filter story | SM agent | Prevents misapplied filter pattern |

## Team Agreements (carry forward)

- Analyzer pitfalls get added to MEMORY.md immediately after a story, before the next one starts ✅
- All planned edge-case fixtures committed before story dev starts ✅
- Zombie action items get a named sprint task or get dropped — no third carry-forward ✅

---

## Readiness Assessment

| Area | Status |
|---|---|
| All stories done | ✅ |
| Build clean | ✅ 0 errors, 0 warnings |
| Tests passing | ✅ 102/102 |
| Token savings gates | ✅ all three filters |
| Codebase health | ✅ solid |
| Epic 4 plan valid | ✅ no revisions needed |
| Epic 3 status key | ⬜ update to `done` (action item #2) |

**Verdict:** Epic 3 fully complete. Clear to begin Epic 4.

---

## Next Steps

1. Add `dotnet_test_zero.txt` fixture + test (action item #1)
2. Flip `epic-3` → `done` in sprint-status.yaml (action item #2)
3. Create Story 4.1 with SM agent's `create-story`

---

*Generated by BMAD retrospective workflow — 2026-03-10*
